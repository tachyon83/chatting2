module.exports = socket => {

    // socket.on('chat.out', chatDto => {
    //     // console.log(chatDto)
    //     socket.to(socket.pos).emit('chat.in', chatDto)
    //     // io.in(socket.pos).emit('chat.in', chatDto)
    // })

}